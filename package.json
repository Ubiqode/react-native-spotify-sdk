{
  "name": "nitro-rn-spotify",
  "private": true,
  "workspaces": [
    "package",
    "example"
  ],
  "scripts": {
    "prebuild": "bun run --cwd package prebuild",
    "build": "bun run --cwd package build",
    "example": "bun run --cwd example start",
    "test": "bun run --cwd package test",
    "clean": "bun run --cwd package clean && bun run --cwd example clean"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
