///
/// HybridSpotifyPlayerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `PlayerState` to properly resolve imports.
namespace margelo::nitro::spotifysdk { struct PlayerState; }
// Forward declaration of `PlaybackOptions` to properly resolve imports.
namespace margelo::nitro::spotifysdk { struct PlaybackOptions; }
// Forward declaration of `RepeatMode` to properly resolve imports.
namespace margelo::nitro::spotifysdk { enum class RepeatMode; }
// Forward declaration of `Track` to properly resolve imports.
namespace margelo::nitro::spotifysdk { struct Track; }

#include <functional>
#include "PlayerState.hpp"
#include <NitroModules/Promise.hpp>
#include "PlaybackOptions.hpp"
#include "RepeatMode.hpp"
#include <vector>
#include "Track.hpp"

namespace margelo::nitro::spotifysdk {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `SpotifyPlayer`
   * Inherit this class to create instances of `HybridSpotifyPlayerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSpotifyPlayer: public HybridSpotifyPlayerSpec {
   * public:
   *   HybridSpotifyPlayer(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSpotifyPlayerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSpotifyPlayerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSpotifyPlayerSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void onPlayerStateChange(const std::function<void(const PlayerState& /* s */)>& cb) = 0;
      virtual std::shared_ptr<Promise<void>> play(const PlaybackOptions& options) = 0;
      virtual std::shared_ptr<Promise<void>> resume() = 0;
      virtual std::shared_ptr<Promise<void>> pause() = 0;
      virtual std::shared_ptr<Promise<void>> seek(double positionMs) = 0;
      virtual std::shared_ptr<Promise<void>> skipNext() = 0;
      virtual std::shared_ptr<Promise<void>> skipPrevious() = 0;
      virtual std::shared_ptr<Promise<void>> toggleShuffle(bool enabled) = 0;
      virtual std::shared_ptr<Promise<void>> setRepeatMode(RepeatMode mode) = 0;
      virtual std::shared_ptr<Promise<PlayerState>> getPlayerState() = 0;
      virtual std::shared_ptr<Promise<std::vector<Track>>> getQueue() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "SpotifyPlayer";
  };

} // namespace margelo::nitro::spotifysdk
