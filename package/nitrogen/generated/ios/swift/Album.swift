///
/// Album.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

import NitroModules

/**
 * Represents an instance of `Album`, backed by a C++ struct.
 */
public typealias Album = margelo.nitro.spotifysdk.Album

public extension Album {
  private typealias bridge = margelo.nitro.spotifysdk.bridge.swift

  /**
   * Create a new instance of `Album`.
   */
  init(id: String, name: String, uri: String, images: [Image]) {
    self.init(std.string(id), std.string(name), std.string(uri), { () -> bridge.std__vector_Image_ in
      var __vector = bridge.create_std__vector_Image_(images.count)
      for __item in images {
        __vector.push_back(__item)
      }
      return __vector
    }())
  }

  var id: String {
    @inline(__always)
    get {
      return String(self.__id)
    }
    @inline(__always)
    set {
      self.__id = std.string(newValue)
    }
  }
  
  var name: String {
    @inline(__always)
    get {
      return String(self.__name)
    }
    @inline(__always)
    set {
      self.__name = std.string(newValue)
    }
  }
  
  var uri: String {
    @inline(__always)
    get {
      return String(self.__uri)
    }
    @inline(__always)
    set {
      self.__uri = std.string(newValue)
    }
  }
  
  var images: [Image] {
    @inline(__always)
    get {
      return self.__images.map({ __item in __item })
    }
    @inline(__always)
    set {
      self.__images = { () -> bridge.std__vector_Image_ in
        var __vector = bridge.create_std__vector_Image_(newValue.count)
        for __item in newValue {
          __vector.push_back(__item)
        }
        return __vector
      }()
    }
  }
}
